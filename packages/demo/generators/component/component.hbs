/**
 * {{FILE_NAME}} {{FILE_DESC}}
 */

import Taro, {
  useAddToFavorites,
  useShareAppMessage,
  useShareTimeline
} from '@tarojs/taro'
import { View } from '@tarojs/components'
import React from "react";
import { XButton } from 'taro-x-ui'
import { handleViewDocs } from './../../utils/viewdocs'

import logo from './../../assets/logo.png'

const X{{pascalCase FILE_NAME}}Index: React.FC = () => {

  // 分享给朋友
  useShareAppMessage(() => {
    return {
      title: 'Taro X UI - {{pascalCase FILE_NAME}}',
      path: '/pages/home/index?source=share',
      imageUrl: logo
    }
  })

  // 分享到朋友圈
  useShareTimeline(() => {
    return {
      title: 'Taro X UI - {{pascalCase FILE_NAME}}',
      query: 'source=timeline',
      imageUrl: logo
    }
  })

  // 收藏
  useAddToFavorites(() => {
    return {
      title: 'Taro X UI - {{pascalCase FILE_NAME}}',
      query: 'source=favorites',
      imageUrl: logo
    }
  })

  return (
    <View></View>
  );
};

export default X{{pascalCase FILE_NAME}}Index;
